package com.chh.flexboxlayoututils.interfaces;

import android.view.View;

public interface setOnItemCheckListener {
    void onChecked(int position, View view);

    void unChecked(int position,View view);
}
